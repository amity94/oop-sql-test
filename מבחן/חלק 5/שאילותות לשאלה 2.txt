--update Courses set avg_grade = (select avg(grade) from grades where grades.student_id = courses.id);
--update Courses set num_of_students = (select count(course_id) from grades where grades.student_id = courses.id);
-- update Courses set highest_grade = (select max(grade) from grades where grades.student_id = courses.id);
